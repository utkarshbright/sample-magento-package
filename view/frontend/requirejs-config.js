var config = {
    paths: {
        sbsimsdk: 'https://nice-glowworm-blessed.ngrok-free.app/dist/sbsimsdk' // Path without .js extension
    },
    shim: {
        sbsimsdk: {
            deps: [] // Add dependencies if required, e.g., ['jquery']
        }
    }
};
